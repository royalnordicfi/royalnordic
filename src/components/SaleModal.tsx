/**
 * @deprecated Replaced by WinterPromoPopup + PromotionAnnouncementBar.
 * Kept as a no-op export so any lingering imports do not break the build.
 */
const SaleModal = () => null

export default SaleModal
